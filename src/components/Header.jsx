import React from 'react';
import logo from "../assets/logo.png"

const Header = () => {
    return (
        <div>
            <img src={logo} alt="" />
            <h2>Header</h2>
        </div>
    );
};

export default Header;